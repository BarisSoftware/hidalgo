import React from "react";

function PassConfirmatioInput(props) {
  <input
    type="password"
    id="passConfirmation"
    placeholder="Confirmación de contraseña"
  ></input>;
}

export default PassConfirmatioInput;
